{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "DevcontainerSchema",
  "title": "Custom Devcontainer Insertion",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the generate."
    },
    "directory": {
      "type": "string",
      "description": "The directory where you want to generate the code."
    },
    "language": {
      "type": "string",
      "description": "The source root path."
    },
    "sourceRoot": {
      "type": "string",
      "description": "The source root path."
    }
  }
}
