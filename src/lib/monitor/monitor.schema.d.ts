import { Path } from '@angular-devkit/core';

export interface MonitorOptions {
  name?: string;
  directory?: string;
  directory?: string;
  language?: string;
  sourceRoot?: string;
  flat?: boolean;
}
